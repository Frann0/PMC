package bll;

import be.Movie;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

public class Searcher {
    /*
    public ObservableList<Movie> search(ObservableList<Movie> startList, String query){
        ObservableList<Movie> results = FXCollections.observableArrayList();
        results.addAll(startList);

        results.removeIf(movie -> !(movie.getTitle().toLowerCase().contains(query.toLowerCase())
                ||movie.getRating().toLowerCase().contains(query.toLowerCase())
                ||movie.getGenres().contains(query.toLowerCase())));

        return results;
    }

     */
}
